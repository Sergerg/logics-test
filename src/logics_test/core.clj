(ns logics-test.core
  ;(:gen-class)
  ;(:require [clojure.core.logic :as l])
  (:use [clojure.core.logic :as l]))

;; LOGIC

(run* [q]
      (== q true))
;;=> (true)



; (defn -main
;   "I don't do a whole lot ... yet."
;   [& args]
;   (println "Hello, World!"))
